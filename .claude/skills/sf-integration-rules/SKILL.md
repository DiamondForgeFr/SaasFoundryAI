---
name: integration-rules
description:
  Integration grammar router — invoke when adding a backend module, a frontend page, an API endpoint, a React Query hook, a form, an RBAC permission, or any cross-cutting wire-up in a
  SaaSFoundry-generated project. Triggers on phrases like "add a module", "add a page", "wire RBAC", "add a hook", "add a form", "add an endpoint", "add a Prisma model", "add an entity", "scaffold a
  CRUD", "create a controller".
allowed-tools: Read, Glob, Grep, Edit, Write, Bash
---

# Integration Rules — How to extend a SaaSFoundry project

This skill is the **integration grammar** for any project generated by `sf new`. When asked to add, wire, or extend something, **read this file first** and route into the relevant sub-guide. Do
**not** re-discover conventions from a blank slate — they are already documented here.

> If you are not sure whether the user's request falls under this skill, the answer is almost always yes whenever they say _add_, _wire_, _create_, or _scaffold_ against the API or the web app.

## How to use this skill

1. **Read the topology** first — this project is either monorepo or multirepo. The on-disk signal: `packages/` exists at the repo root → monorepo. Otherwise multirepo. The manifest
   (`.saasfoundry.json` → `structure`) is the source of truth — confirm it before making decisions that depend on topology (where types live, ShadCN imports, API client, …).
2. **Pick the sub-guide** from the table below by intent.
3. **Follow it strictly** — these conventions exist so contributions stay consistent across modules and so future tooling (codegen, drift-guards, Migration framework) keeps working.
4. **When in doubt**, run `sf status --claude-friendly --no-network` to confirm topology + installed modules before editing.

## Sub-guide router

| Intent                                               | Sub-guide                              |
| ---------------------------------------------------- | -------------------------------------- |
| Add a backend module (controller + service + DTO)    | `backend.md` — module structure        |
| Add a Prisma model / migrate the schema              | `backend.md` — Prisma                  |
| Wire RBAC (`@RequirePermissions` + guards)           | `backend.md` — RBAC                    |
| Add Zod validation that the frontend will reuse      | `backend.md` — validation              |
| Write backend tests (unit + E2E)                     | `backend.md` — tests                   |
| Add a page (private vs public, layout, lazy-load)    | `frontend.md` — pages & routing        |
| Add a React Query hook                               | `frontend.md` — API hooks              |
| Add a form (React Hook Form + Zod)                   | `frontend.md` — forms                  |
| Add ShadCN component / theme tokens                  | `frontend.md` — UI primitives          |
| Add i18n translations (YAML keys)                    | `frontend.md` — i18n                   |
| Decide where a shared type / schema / constant lives | `topology.md` — mono vs multi          |
| Decide whether a generated typed API client is wired | `topology.md` — api-client (mono only) |
| Resolve drift-guard test failure                     | `topology.md` — drift discipline       |

## Topology reminder (read before any cross-app change)

- **Monorepo**: `packages/shared-types`, `packages/shared-validation`, `packages/shared-config`, `packages/ui-primitives`, `packages/api-client` are the canonical homes for cross-cutting code. Apps
  consume via `@<project>/<package>` imports (or via vendored mirrors + `@shared-{types,validation}/*` aliases for the dual-track packages).
- **Multirepo**: each app is stand-alone — the same shapes are inlined or vendored per side. **Do not import from `packages/`** because the directory does not exist.

The full mono-vs-multi divergence tables live in `topology.md` — read them before making changes that touch types, validation, primitives, or the API client.

## Discipline rules (don't skip)

- **Never invent a parallel install path** for a module — extend the existing installer (`src/installers/<module>.installer.ts` in the SaaSFoundry CLI; for app-internal modules, mirror the closest
  existing module's structure exactly).
- **Never bypass drift-guard tests** — when a vendored mirror or an inlined multirepo literal triggers a Jest failure, fix the divergence on both sides; do not disable the test.
- **Never add comments that explain WHAT** — names should already do that. Only add a comment for non-obvious WHY (hidden constraint, subtle invariant, workaround for a specific bug).
- **Never half-finish a wiring** — if you add a controller, also wire the module, RBAC, OpenAPI tag, DTO, tests, and (for monorepo) regenerate the api-client.
- **Always confirm topology** before editing files that exist in both `apps/<app>/src/shared-*` and `packages/shared-*` — they may need to change in lockstep.

## When the sub-guide is missing

If a sub-guide referenced above does not yet exist in this directory, the corresponding story in Epic #305 has not landed. In that case:

1. Tell the user which sub-guide is missing.
2. Fall back to inspecting the closest existing module (backend) or page (frontend) and **mirror its structure exactly** — same file names, same import order, same test pattern.
3. Surface a follow-up note so the user can update the sub-guide later.
