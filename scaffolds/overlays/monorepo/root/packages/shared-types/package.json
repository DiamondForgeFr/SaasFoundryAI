{
  "name": "@{{PROJECT_NAME}}/shared-types",
  "version": "0.0.1",
  "description": "Shared TypeScript types consumed by both apps/api and apps/web",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
